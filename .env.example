# Innominatus Graph SDK - Environment Configuration

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite Configuration (Default - No setup required)
# Just run the application, it will create a local .db file
USE_SQLITE=1

# PostgreSQL Configuration (Production)
# Uncomment and configure these variables to use PostgreSQL instead of SQLite
#DB_HOST=localhost
#DB_PORT=5432
#DB_USER=postgres
#DB_PASSWORD=your-password-here
#DB_NAME=idp_orchestrator
#DB_SSLMODE=disable

# =============================================================================
# Example Usage
# =============================================================================

# SQLite mode (default):
#   go run examples/demo/main.go

# PostgreSQL mode:
#   DB_PASSWORD=secret go run examples/demo/main.go

# Explicit SQLite mode:
#   USE_SQLITE=1 go run examples/demo/main.go

# =============================================================================
# Notes
# =============================================================================

# - SQLite is recommended for development and testing
# - PostgreSQL is recommended for production use
# - If DB_PASSWORD is set and USE_SQLITE is not set, PostgreSQL will be used
# - Auto-migration runs on startup for both database types
